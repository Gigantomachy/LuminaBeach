version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: dockerfiles/Dockerfile
    ports:
      - "3001:3001"
    volumes:
      - ./backend:/app
      - /app/node_modules  # Anonymous volume
    environment:
      - NODE_ENV=development
    env_file:
      - ./backend/.env